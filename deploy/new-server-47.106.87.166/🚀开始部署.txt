╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║          🚀 新服务器部署 - 超简单 2 步搞定！                  ║
║                                                               ║
║   服务器: 47.106.87.166                                       ║
║   数据库: 阿里云 RDS (已配置)                                 ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝


┌───────────────────────────────────────────────────────────────┐
│  步骤 1: SSH 登录服务器                                       │
└───────────────────────────────────────────────────────────────┘

在你的电脑终端执行：

    ssh root@47.106.87.166

输入密码登录


┌───────────────────────────────────────────────────────────────┐
│  步骤 2: 复制以下命令，在服务器上执行                         │
└───────────────────────────────────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
方式 A: 手动执行（完全可控，推荐）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 1. 克隆项目
cd /www/wwwroot
git clone -b main https://github.com/xuxiang6/shumu.git
cd shumu

# 2. 生成 JWT 密钥（复制输出的字符串）
openssl rand -base64 32

# 3. 创建配置文件（将上面的密钥粘贴到 JWT_SECRET）
cat > .env << 'EOF'
DATABASE_URL="mysql://xitongquanju:b4FFS6kVGKV4jV@guishumu999666.rwlb.rds.aliyuncs.com:3306/sm_shangping"
JWT_SECRET="粘贴上面生成的随机字符串"
NODE_ENV=production
PORT=5000
NEXT_PUBLIC_API_BASE=/api
LOG_LEVEL=info
EOF

# 4. 构建并启动
docker-compose build
docker-compose up -d

# 5. 验证
docker-compose ps
curl http://127.0.0.1:5000/health

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
方式 B: 一键自动部署（最简单）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

cd /tmp
curl -o deploy.sh https://raw.githubusercontent.com/xuxiang6/shumu/main/deploy/new-server-47.106.87.166/一键部署脚本.sh
chmod +x deploy.sh
bash deploy.sh


┌───────────────────────────────────────────────────────────────┐
│  完成！现在可以访问了                                         │
└───────────────────────────────────────────────────────────────┘

临时访问（需要在宝塔放行 3000 和 5000 端口）：

    Web 前端: http://47.106.87.166:3000
    API: http://47.106.87.166:5000/health


┌───────────────────────────────────────────────────────────────┐
│  可选：配置域名（推荐）                                       │
└───────────────────────────────────────────────────────────────┘

如果你有域名，在宝塔面板：

1. 网站 → 添加站点 → 输入域名
2. 网站设置 → 配置文件
3. 参考文件：
   /www/wwwroot/shumu/deploy/new-server-47.106.87.166/3-nginx-config.conf


┌───────────────────────────────────────────────────────────────┐
│  常用命令                                                     │
└───────────────────────────────────────────────────────────────┘

cd /www/wwwroot/shumu

# 查看日志
docker-compose logs -f

# 重启服务
docker-compose restart

# 停止服务
docker-compose down

# 更新代码
git pull origin main
docker-compose down
docker-compose build --no-cache
docker-compose up -d


┌───────────────────────────────────────────────────────────────┐
│  遇到问题？                                                   │
└───────────────────────────────────────────────────────────────┘

查看详细文档：
    cat /www/wwwroot/shumu/deploy/new-server-47.106.87.166/README.md

查看快速指南：
    cat /www/wwwroot/shumu/deploy/new-server-47.106.87.166/快速开始.txt


╔═══════════════════════════════════════════════════════════════╗
║                       🎉 祝部署顺利！                         ║
╚═══════════════════════════════════════════════════════════════╝

